mutation Login($password: String!, $username: String!) {
  login(input: { username: $username, password: $password }) {
    access_token
    user {
      id
      name
      email
      email_verified_at
      created_at
      updated_at
    }
  }
}
